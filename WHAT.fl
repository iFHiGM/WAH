`begin->config->hello
    `begin->step->one
        `action->execute`
              `->hello_lilei.sh`
    one->step->end`
    `begin->step->two
	`action->execute
	       ->hello_too_hanmeimei.sh`
    two->step->end`
    `begin->step->three
	`action->execute
	       ->goodbye.sh`
    `three->step->end`
hello->config->end`  
